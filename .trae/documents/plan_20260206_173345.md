I will update `services/database.ts` to ensure that reminders created by the app (which may use `ownerId` instead of or in addition to `userId`) are correctly fetched and displayed in the web application.

1. **Modify** **`services/database.ts`**:

   * Update `listenToReminders` to add a new query/listener for the root `reminders` collection filtering by `ownerId`.

   * Ensure the data from this new listener is merged with existing reminders.

   * Verify that field normalization (mapping `price`/`value` to `amount`, `title`/`name` to \`description

